---
phase: 07-dashboard-rebuild
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - app/web/templates/partials/performance.html
  - app/web/templates/partials/props.html
  - app/web/templates/partials/games.html
  - app/web/templates/partials/value_bets.html
  - app/web/templates/partials/arbs.html
  - app/web/app.py
autonomous: false
requirements: [R5.4, R5.7]

must_haves:
  truths:
    - "Performance page shows P&L bar chart rendered by Chart.js"
    - "Performance page shows KPIs (win rate, ROI, net P&L, total bets) server-side"
    - "Player props page renders server-side via Jinja2"
    - "Active tab content auto-refreshes every 60 seconds via HTMX polling"
    - "HTMX polling replaces the current JS setInterval refresh mechanism"
    - "Dashboard visually passes professional quality bar"
  artifacts:
    - path: "app/web/templates/partials/performance.html"
      provides: "Server-rendered performance dashboard with Chart.js"
      contains: "Chart("
    - path: "app/web/templates/partials/props.html"
      provides: "Server-rendered player props display"
    - path: "app/web/templates/partials/games.html"
      provides: "HTMX polling trigger for auto-refresh"
      contains: "hx-trigger"
    - path: "app/web/templates/partials/value_bets.html"
      provides: "HTMX polling trigger for auto-refresh"
      contains: "hx-trigger"
    - path: "app/web/templates/partials/arbs.html"
      provides: "HTMX polling trigger for auto-refresh"
      contains: "hx-trigger"
  key_links:
    - from: "app/web/templates/partials/performance.html"
      to: "Chart.js CDN"
      via: "canvas element + Chart constructor"
      pattern: "new Chart"
    - from: "app/web/templates/partials/games.html"
      to: "app/web/app.py /games route"
      via: "hx-trigger every 60s polling"
      pattern: 'hx-trigger="every 60s"'
    - from: "app/web/templates/partials/value_bets.html"
      to: "app/web/app.py /value-bets route"
      via: "hx-trigger every 60s polling"
      pattern: 'hx-trigger="every 60s"'
    - from: "app/web/templates/partials/arbs.html"
      to: "app/web/app.py /arbs route"
      via: "hx-trigger every 60s polling"
      pattern: 'hx-trigger="every 60s"'
    - from: "app/web/templates/partials/performance.html"
      to: "app/web/app.py /performance route"
      via: "hx-trigger every 60s polling"
      pattern: 'hx-trigger="every 60s"'
    - from: "app/web/templates/partials/props.html"
      to: "app/web/app.py /props route"
      via: "hx-trigger every 60s polling"
      pattern: 'hx-trigger="every 60s"'
---

<objective>
Complete the remaining partials (performance with Chart.js, player props), add HTMX polling for auto-refresh, and visually verify the complete dashboard.

Purpose: Finishes the dashboard rebuild with the performance tracking page (Chart.js charts), props page, and replaces JS setInterval with declarative HTMX polling.
Output: All 5 tab pages fully functional with server-side rendering, auto-refresh via HTMX, professional visual quality.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-rebuild/07-CONTEXT.md
@.planning/phases/07-dashboard-rebuild/07-RESEARCH.md
@.planning/phases/07-dashboard-rebuild/07-01-SUMMARY.md

<interfaces>
<!-- Performance data structure from app.py _build_performance_data() -->
```python
# Performance data dict:
{
    "settled_bets": int,
    "pending_bets": int,
    "total_bets": int,
    "wins": int,
    "losses": int,
    "win_rate": float,  # 0.0-1.0
    "roi_pct": float,
    "net_profit": float,
    "total_staked": float,
    "by_month": [{"month": str, "profit": float, "roi": float, "bets": int}],
    "by_book": [{"book": str, "bets": int, "profit": float, "roi": float}],
    "recent_bets": [{"side": str, "game": str, "odds": float, "stake": float, "profit": float, "result": str}],
}

# Props data (in dashboard data dict):
# data["props"] = [{"player": str, "team": str, "stat": str, "line": float, "over_odds": float, ...}]
```

<!-- Chart.js CDN already in base.html head from Plan 01 -->
```html
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.js"></script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert performance and props partials to server-side rendering, update app.py route</name>
  <files>
    app/web/templates/partials/performance.html
    app/web/templates/partials/props.html
    app/web/app.py
  </files>
  <action>
**1. Rewrite partials/performance.html** to server-side Jinja2 + Chart.js:

  - Render KPI strip server-side (win rate, ROI, net P&L, total bets) using `data` template variable from the route handler
  - Render by-book table server-side: `{% for book in data.by_book %}<tr>...</tr>{% endfor %}`
  - Render recent bets list server-side: `{% for bet in data.recent_bets %}`
  - Add Chart.js bar chart for monthly P&L:
    ```html
    <canvas id="pl-chart" height="200"></canvas>
    <script>
    (function() {
      const ctx = document.getElementById('pl-chart');
      if (!ctx) return;
      const months = {{ data.by_month | tojson }};
      new Chart(ctx, {
        type: 'bar',
        data: {
          labels: months.map(m => m.month),
          datasets: [{
            data: months.map(m => m.profit),
            backgroundColor: months.map(m => m.profit >= 0
              ? 'rgba(16,185,129,0.7)' : 'rgba(239,68,68,0.7)'),
            borderRadius: 4,
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            y: { grid: { color: 'rgba(51,65,85,0.3)' }, ticks: { color: 'var(--text-2)' } },
            x: { grid: { display: false }, ticks: { color: 'var(--text-2)' } }
          }
        }
      });
    })();
    </script>
    ```
  - Handle empty state: `{% if data.settled_bets == 0 %}<div class="empty-state">No tracking data yet...</div>{% endif %}`
  - Remove the old `renderPerf`, `showPerfError`, `loadPerf` JS functions entirely
  - The performance route handler must pass performance data directly (not require a separate fetch)

  **Update the /performance route in app.py** to include performance data in the template context:
  ```python
  @app.get("/performance", response_class=HTMLResponse)
  def performance_page(request: Request, demo: str = "0"):
      perf_data = _build_performance_data()
      if request.headers.get("HX-Request") == "true":
          return templates.TemplateResponse(
              request=request, name="partials/performance.html",
              context={"data": perf_data},
          )
      return templates.TemplateResponse(
          request=request, name="base.html",
          context={"data": perf_data, "active_tab": "performance"},
      )
  ```

**2. Rewrite partials/props.html** to server-side Jinja2:
  - Render player props table server-side from `data.props` (or from dedicated props data)
  - Show player name, team, stat type, line, over/under odds, edge
  - Empty state: `{% if not data.props %}<div class="empty-state">No player prop edges found</div>{% endif %}`
  - Minimal JS (none needed for props display)

**3. Ensure Chart.js CDN is in base.html head** (should already be there from Plan 01, verify):
  ```html
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.js"></script>
  ```
  If Plan 01 did not add it, add it now.

**IMPORTANT:** Chart.js scripts in the performance partial must work both on initial page load AND after HTMX tab switch. Use an immediately-invoked function in the partial's `<script>` block (not `DOMContentLoaded` or `htmx:afterSwap` event listener, since the IIFE runs when the partial HTML is injected by HTMX).
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>
    - Performance page renders KPIs, bar chart, by-book table, recent bets server-side
    - Chart.js bar chart shows monthly P&L data
    - Props page renders server-side
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add HTMX polling to all 5 partials and remove JS setInterval</name>
  <files>
    app/web/templates/partials/games.html
    app/web/templates/partials/value_bets.html
    app/web/templates/partials/arbs.html
    app/web/templates/partials/performance.html
    app/web/templates/partials/props.html
  </files>
  <action>
Add HTMX polling to all 5 partials to replace the JS setInterval auto-refresh mechanism:

**1. In each partial,** wrap the content in a div with HTMX polling trigger:

For games.html:
```html
<div hx-get="/games?demo={{ data.mode }}&region={{ data.region|default('qc') }}&bankroll={{ data.bankroll|default(1000) }}&min_edge={{ data.min_edge|default(2.0) }}&min_ev={{ data.min_ev|default(0.02) }}"
     hx-trigger="every 60s" hx-target="this" hx-swap="innerHTML">
  <!-- existing games partial content -->
</div>
```

For value_bets.html:
```html
<div hx-get="/value-bets?demo={{ data.mode }}" hx-trigger="every 60s" hx-target="this" hx-swap="innerHTML">
  <!-- existing value_bets partial content -->
</div>
```

For arbs.html:
```html
<div hx-get="/arbs?demo={{ data.mode }}" hx-trigger="every 60s" hx-target="this" hx-swap="innerHTML">
  <!-- existing arbs partial content -->
</div>
```

For performance.html:
```html
<div hx-get="/performance?demo={{ data.mode|default('live') }}" hx-trigger="every 60s" hx-target="this" hx-swap="innerHTML">
  <!-- existing performance partial content -->
</div>
```

For props.html:
```html
<div hx-get="/props?demo={{ data.mode }}" hx-trigger="every 60s" hx-target="this" hx-swap="innerHTML">
  <!-- existing props partial content -->
</div>
```

**2. Key design notes** (per RESEARCH.md pitfall #2):
  - Put the `hx-trigger="every 60s"` on a div INSIDE each partial so that when the partial is swapped out (tab switch), polling stops automatically
  - The games partial must propagate region/bankroll/min_edge/min_ev params in its polling URL
  - Propagate demo mode via query params in the hx-get URL for all partials

**3. Remove any remaining JS `setInterval` auto-refresh** from all partials and base.html:
  - Search for `setInterval` in all template files and remove
  - Search for `setTimeout` used for refresh loops and remove
  - The HTMX polling is the sole refresh mechanism going forward
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>
    - All 5 partials have hx-trigger="every 60s" for auto-refresh
    - No JS setInterval remains in any template file
    - Polling URLs propagate demo mode and relevant query params
    - Polling stops when switching tabs (partial swapped out)
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <what-built>
    Complete multi-page dashboard rebuild:
    - 5 tab pages (Games, Value Bets, Arbs, Performance, Props)
    - HTMX tab navigation with bookmarkable URLs
    - Server-side Jinja2 rendering (no JS DOM building)
    - Chart.js P&L chart on Performance page
    - HTMX 60s auto-refresh polling
    - Mobile-responsive layout
    - Glassmorphism and hover effects
    - Dark/light theme preserved
  </what-built>
  <how-to-verify>
    1. Start the server: `python -m uvicorn app.web.app:app --port 8080`
    2. Open http://localhost:8080/?demo=1
    3. **Tab navigation:** Click each tab (Games, Value Bets, Arbs, Performance, Props). Verify:
       - Content swaps smoothly (no full page reload)
       - URL updates in address bar (e.g., /value-bets)
       - Active tab is highlighted
    4. **Direct navigation:** Open http://localhost:8080/value-bets?demo=1 directly. Verify full page loads with Value Bets tab active.
    5. **Refresh:** Press F5 on any tab URL. Verify same tab remains active after refresh.
    6. **Performance page:** Check Chart.js bar chart renders, KPIs display correctly.
    7. **Mobile:** Open browser dev tools, toggle device toolbar to 375px width. Verify:
       - Tabs scroll horizontally
       - Game cards stack vertically
       - Modal is near-full-screen
       - Everything is tappable (no tiny targets)
    8. **Theme:** Toggle dark/light theme. Verify all pages respect theme.
    9. **Auto-refresh:** Open Network tab, wait 60 seconds. Verify HTMX polling request fires.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` full suite green
- All 5 tabs render correctly with server-side content
- Chart.js renders on performance page
- HTMX polling fires every 60s (visible in Network tab)
- No JS setInterval in any template source
- Mobile layout works at 375px, 768px viewports
- Dark/light theme works across all tabs
</verification>

<success_criteria>
- Performance page has Chart.js bar chart for monthly P&L
- Props page renders player prop edges
- All 5 partials use hx-trigger="every 60s" for auto-refresh
- Dashboard passes visual inspection for professional quality
- Complete migration from client-side JS rendering to server-side Jinja2
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-rebuild/07-03-SUMMARY.md`
</output>
