---
phase: 07-dashboard-rebuild
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - app/web/templates/macros/cards.html
  - app/web/templates/partials/games.html
  - app/web/templates/partials/value_bets.html
  - app/web/templates/partials/arbs.html
  - app/web/templates/base.html
autonomous: true
requirements: [R5.4, R5.6]

must_haves:
  truths:
    - "Game cards render server-side via Jinja2 macros without client-side JS DOM building"
    - "Value bets table renders server-side with sortable columns"
    - "Arb cards render server-side via Jinja2"
    - "Dashboard is mobile-responsive at 480px, 768px, and 1024px breakpoints"
    - "Game cards show probabilities as the dominant visual element (Polymarket-inspired)"
    - "Value indicator uses green glow border on game cards"
    - "Nav tabs scroll horizontally on mobile with no hamburger menu"
    - "All touch targets are minimum 44px"
  artifacts:
    - path: "app/web/templates/macros/cards.html"
      provides: "Jinja2 macros: game_card, value_bet_row, arb_card, stat_pill, badge"
      contains: "macro game_card"
    - path: "app/web/templates/partials/games.html"
      provides: "Server-rendered game cards grid with KPI strip and book chips"
      contains: "game_card("
    - path: "app/web/templates/partials/value_bets.html"
      provides: "Server-rendered value bets table and plays cards"
      contains: "value_bet_row("
    - path: "app/web/templates/partials/arbs.html"
      provides: "Server-rendered arbitrage cards"
      contains: "arb_card("
  key_links:
    - from: "app/web/templates/partials/games.html"
      to: "app/web/templates/macros/cards.html"
      via: "Jinja2 import and macro calls"
      pattern: 'from "macros/cards.html" import'
    - from: "app/web/templates/partials/value_bets.html"
      to: "app/web/templates/macros/cards.html"
      via: "Jinja2 import and macro calls"
      pattern: 'from "macros/cards.html" import'
    - from: "app/web/templates/partials/arbs.html"
      to: "app/web/templates/macros/cards.html"
      via: "Jinja2 import and macro calls"
      pattern: 'from "macros/cards.html" import'
    - from: "app/web/app.py"
      to: "app/web/templates/partials/games.html"
      via: "TemplateResponse with data context"
      pattern: "partials/games.html"
---

<objective>
Migrate the games, value bets, and arbs partials from client-side JavaScript DOM building to server-side Jinja2 rendering, and add full mobile-responsive CSS.

Purpose: Eliminates ~600 lines of JS template literal code by rendering HTML server-side. Adds mobile breakpoints for professional presentation.
Output: Three fully server-rendered partials using Jinja2 macros, mobile-responsive layout.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-rebuild/07-CONTEXT.md
@.planning/phases/07-dashboard-rebuild/07-RESEARCH.md
@.planning/phases/07-dashboard-rebuild/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: the template structure and route handlers -->

Template structure:
```
app/web/templates/
  base.html              # Shell with nav tabs, CSS, HTMX script
  partials/
    games.html           # Games tab (currently has client-side JS rendering)
    value_bets.html      # Value bets tab (currently has client-side JS rendering)
    arbs.html            # Arbs tab (currently has client-side JS rendering)
    performance.html     # Performance tab
    props.html           # Props tab
  macros/
    cards.html           # Placeholder macros from Plan 01
```

Route handlers pass `data` dict and `data_json` to templates:
```python
# data dict structure (from _build_demo_dashboard / _build_live_dashboard):
{
    "games": [{"home": str, "away": str, "books": {...}, "model_prob_home": float, ...}],
    "value_bets": [{"side": str, "team": str, "book": str, "odds": float, "edge": float, ...}],
    "arbs": [{"home_team": str, "away_team": str, "profit_pct": float, ...}],
    "books": [str],
    "mode": str,
    "summary": {...},
    "polymarket": [...],
    "props": [...],
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Jinja2 macros and convert games partial to server-side rendering</name>
  <files>
    app/web/templates/macros/cards.html
    app/web/templates/partials/games.html
  </files>
  <action>
**1. Build out macros/cards.html** with full Jinja2 macros replacing the JS `renderGameCard`, `renderBets` functions:

- `game_card(game, idx)` macro -- Polymarket-inspired design per CONTEXT.md:
  - Probabilities as dominant visual element (large font, bold)
  - Green glow border (`box-shadow: 0 0 15px var(--green-glow)`) on cards with value bets
  - Team badges, game time, injury indicators
  - Sparkline placeholder (keep as inline SVG element with data attributes for JS to populate)
  - Book odds grid showing each bookmaker's odds
  - Click handler via data attributes for modal (e.g., `data-game-idx="{{ idx }}"`)
  - Glassmorphism touch: `backdrop-filter: blur(12px)` on card background per CONTEXT.md
  - Card hover lift: `transform: translateY(-2px)` on `:hover` with `transition: transform 0.2s`

- `stat_pill(label, value, color="default")` macro -- for KPI items

- `value_bet_row(bet)` macro -- table row for value bets table:
  - Team, book, odds, model prob, edge%, EV, Kelly size
  - Color-coded edge (green for positive)
  - Deep link to sportsbook via `build_sportsbook_url` (pass URL from backend data)

- `arb_card(arb)` macro -- card for arbitrage opportunities:
  - Teams, books, odds, profit percentage
  - Stakes for each leg
  - Guaranteed profit highlight

- `badge(text, variant="default")` macro -- reusable badge component

**2. Rewrite partials/games.html** to server-side Jinja2 rendering:
  - Import macros: `{% from "macros/cards.html" import game_card, stat_pill %}`
  - Render KPI strip server-side using `data.summary` values
  - Render book filter chips server-side from `data.books` list
  - Render game cards grid using `{% for game in data.games %} {{ game_card(game, loop.index0) }} {% endfor %}`
  - Remove the `fetch('/api/dashboard')` call and the JS `init()` function that builds DOM from JSON
  - Keep only essential JS: sparkline rendering (buildSparkPolyline), book chip filter toggle (client-side show/hide), modal trigger delegation
  - Add empty state: `{% if not data.games %}<div class="empty-state">No games tonight</div>{% endif %}`
  - Keep the controls row (region, bankroll, min_edge, min_ev) but wire them as query params on the partial's self-refresh URL

**CRITICAL:** The goal is to eliminate client-side JS DOM building for games. The partial renders from `data` (the Python dict) via Jinja2, not from `data_json` (the JSON string) via JavaScript.

**CSS notes per CONTEXT.md locked decisions:**
- Keep existing CSS variable system -- do NOT add Tailwind
- Add glassmorphism: `backdrop-filter: blur(12px)` on game card panels
- Add micro-interactions: card hover lifts (`translateY(-2px)`), smooth transitions
- Count-up animations on KPI values (use CSS `@keyframes` or a tiny inline JS counter)
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>
    - macros/cards.html has game_card, value_bet_row, arb_card, stat_pill, badge macros
    - games.html renders game cards via Jinja2 (no JS DOM building)
    - Dashboard visual output matches or improves on previous version for games tab
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert value bets and arbs partials to server-side rendering</name>
  <files>
    app/web/templates/partials/value_bets.html
    app/web/templates/partials/arbs.html
  </files>
  <action>
**1. Rewrite partials/value_bets.html** to server-side rendering:
  - Import macros: `{% from "macros/cards.html" import value_bet_row %}`
  - Render "Today's Plays" cards section server-side
  - Render value bets table server-side with `{% for bet in data.value_bets %}`
  - Keep client-side JS only for column sort functionality (simple table sort, no server round-trip -- data set is <50 rows)
  - Add empty state for no value bets: `{% if not data.value_bets %}<div class="empty-state">No value bets found</div>{% endif %}`
  - Remove JS DOM building code (`renderBets` function and related)

**2. Rewrite partials/arbs.html** to server-side rendering:
  - Import macros: `{% from "macros/cards.html" import arb_card %}`
  - Render arb cards server-side with `{% for arb in data.arbs %}`
  - Minimal JS (just card click interactions if any)
  - Add empty state for no arbs: `{% if not data.arbs %}<div class="empty-state">No arbitrage opportunities</div>{% endif %}`

**IMPORTANT:** After this task, the `data_json` context variable should no longer be needed by value_bets or arbs partials. These partials now render from `data` (the Python dict) via Jinja2.
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>
    - value_bets.html renders table via Jinja2 (no JS DOM building)
    - arbs.html renders cards via Jinja2 (no JS DOM building)
    - ~300 lines of JS template literal code eliminated from these two partials
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Add mobile-responsive CSS breakpoints and polish existing styles</name>
  <files>
    app/web/templates/base.html
  </files>
  <action>
Add comprehensive mobile-responsive CSS to the base.html shell's `<style>` block. Per CONTEXT.md locked decisions:

**1. Mobile breakpoints (add to existing CSS in base.html):**

```css
/* Tablet: 768px */
@media (max-width: 768px) {
  .app { padding: 0 16px 32px; }
  .header { flex-direction: column; align-items: flex-start; }
  .kpi-strip { grid-template-columns: repeat(2, 1fr); }
  .games-grid { grid-template-columns: 1fr; }
  .value-table { font-size: 13px; }
  .value-table th:nth-child(n+5), .value-table td:nth-child(n+5) { display: none; }
  .controls { flex-wrap: wrap; }
}

/* Phone: 480px */
@media (max-width: 480px) {
  .app { padding: 0 12px 24px; }
  .game-card { min-width: unset; }
  .kpi-strip { grid-template-columns: 1fr 1fr; gap: 8px; }
  .kpi { padding: 12px; }
  .kpi-value { font-size: 20px; }
  .tab { padding: 10px 14px; font-size: 12px; }
  .modal-inner { width: 95%; max-height: 90vh; padding: 16px; }
  .brand h1 { font-size: 17px; }
  .header-actions { flex-wrap: wrap; }
}
```

**2. Ensure existing responsive CSS (line ~848 in current base.html) is preserved and extended:**
- Game cards grid: change from current `minmax(340px, 1fr)` to `minmax(300px, 1fr)` on mobile per CONTEXT.md
- KPI strip: 2-column grid on mobile (currently may be overflow)
- Controls row: wrap on mobile
- Value bets table: hide less-important columns on mobile (EV, Kelly columns)
- Modal: near-full-screen on mobile (`width: 95%; max-height: 90vh`)

**3. Touch-friendly sizing:**
- All interactive elements (tabs, buttons, book chips, controls): `min-height: 44px`
- Book filter chips: add `min-height: 44px; min-width: 44px;` for touch targets
- Increase tap target on game cards for mobile

**4. Tab navigation mobile CSS:**
- Horizontal scroll with overflow (per CONTEXT.md: no hamburger menu)
- `overflow-x: auto; -webkit-overflow-scrolling: touch; scrollbar-width: none;`
- Hide scrollbar: `.tab-nav::-webkit-scrollbar { display: none; }`

**5. Polish existing styles (Claude's discretion per CONTEXT.md):**
- Tighten spacing where too loose
- Ensure consistent border-radius usage (var(--radius), var(--radius-sm))
- Verify dark/light theme consistency across all new CSS
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>
    - Mobile breakpoints at 480px, 768px, 1024px in CSS
    - Game cards reflow to single column on mobile
    - KPI strip becomes 2-column grid on mobile
    - Tab nav scrolls horizontally on mobile (no hamburger)
    - All touch targets >= 44px
    - Modal is near-full-screen on mobile
    - Value bets table hides non-essential columns on small screens
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/web/ -x -q` passes
- `python -m pytest tests/ -x -q` full suite green
- Games tab shows Jinja2-rendered game cards (no JS template literals in source)
- Value bets table renders server-side
- Arb cards render server-side
- Dashboard is usable at 375px viewport width (phone)
- Dashboard is usable at 768px viewport width (tablet)
</verification>

<success_criteria>
- Client-side JS DOM building eliminated for games, value bets, and arbs sections
- Jinja2 macros provide reusable card/row/pill components
- Mobile-responsive layout works at all three breakpoints
- Glassmorphism and hover effects present on game cards
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-rebuild/07-02-SUMMARY.md`
</output>
