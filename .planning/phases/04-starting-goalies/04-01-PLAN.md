---
phase: 04-starting-goalies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/dailyfaceoff.py
  - app/data/nhl_api.py
  - app/data/goalie_resolver.py
  - tests/data/test_dailyfaceoff.py
  - tests/data/test_goalie_resolver.py
autonomous: true
requirements: [R3.1, R3.3]

must_haves:
  truths:
    - "DailyFaceoff scraper returns goalie names and confirmation status for today's games"
    - "NHL API gamecenter endpoint returns per-goalie season stats for a specific game"
    - "3-tier resolution correctly picks confirmed > likely > gp_leader"
    - "When DailyFaceoff is down or returns empty, fallback to GP-leader works without error"
  artifacts:
    - path: "app/data/dailyfaceoff.py"
      provides: "DailyFaceoff __NEXT_DATA__ JSON scraper"
      exports: ["fetch_dailyfaceoff_starters"]
    - path: "app/data/nhl_api.py"
      provides: "fetch_game_goalies from gamecenter/landing endpoint"
      exports: ["fetch_game_goalies"]
    - path: "app/data/goalie_resolver.py"
      provides: "3-tier goalie resolution logic"
      exports: ["resolve_starter", "resolve_all_starters"]
    - path: "tests/data/test_dailyfaceoff.py"
      provides: "DailyFaceoff scraper unit tests"
      min_lines: 60
    - path: "tests/data/test_goalie_resolver.py"
      provides: "Goalie resolver unit tests"
      min_lines: 60
  key_links:
    - from: "app/data/goalie_resolver.py"
      to: "app/data/dailyfaceoff.py"
      via: "imports fetch_dailyfaceoff_starters"
      pattern: "from app\\.data\\.dailyfaceoff import"
    - from: "app/data/goalie_resolver.py"
      to: "app/data/nhl_api.py"
      via: "imports infer_likely_starter"
      pattern: "from app\\.data\\.nhl_api import"
---

<objective>
Build the goalie data fetching and resolution layer: DailyFaceoff scraper, NHL API gamecenter goalie enrichment, and the 3-tier resolution function that downstream pipeline integration will consume.

Purpose: Provide confirmed starting goalie data to replace the GP-leader heuristic, with graceful fallback when confirmations are unavailable.
Output: Three production modules (dailyfaceoff.py, nhl_api.py extension, goalie_resolver.py) with comprehensive tests.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-starting-goalies/04-CONTEXT.md
@.planning/phases/04-starting-goalies/04-RESEARCH.md

@app/data/nhl_api.py
@app/core/models.py

<interfaces>
<!-- Existing code the executor needs -->

From app/data/nhl_api.py:
```python
NHL_API_BASE = "https://api-web.nhle.com/v1"

def _fetch_json(url: str, timeout: int = 15, retries: int = 2) -> dict:
    """Fetch url and return parsed JSON. Returns {} on any error."""

def infer_likely_starter(team_code: str, goalie_stats: list[dict]) -> dict | None:
    """GP-leader heuristic. Returns dict with player_name, team_code, games_played, save_pct, gaa, wins."""

def fetch_goalie_stats(season: str = "20242025") -> list[dict]:
    """Returns list of goalie dicts with player_name, team_code, games_played, save_pct, gaa, wins."""
```

From app/core/models.py:
```python
@dataclass(frozen=True)
class TeamMetrics:
    starter_save_pct: float = 0.0
    starter_gaa: float = 0.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DailyFaceoff scraper and NHL API gamecenter goalie enrichment</name>
  <files>app/data/dailyfaceoff.py, app/data/nhl_api.py, tests/data/test_dailyfaceoff.py</files>
  <behavior>
    - Test: fetch_dailyfaceoff_starters parses __NEXT_DATA__ JSON and returns list of dicts with home_goalie, away_goalie, home_status, away_status, home_save_pct, away_save_pct, home_team (abbrev), away_team (abbrev)
    - Test: confirmed status maps correctly (Confirmed -> "confirmed", Likely -> "likely", null -> "unconfirmed")
    - Test: DailyFaceoff slugs correctly map to NHL team abbreviations via SLUG_TO_ABBREV dict (all 32 teams)
    - Test: malformed/missing __NEXT_DATA__ returns empty list (no crash)
    - Test: HTTP timeout/error returns empty list with warning log
    - Test: fetch_game_goalies returns dict keyed by team abbrev with list of goalie dicts (player_id, name, games_played, save_pct, gaa)
    - Test: fetch_game_goalies returns {} on API failure
  </behavior>
  <action>
    Create `app/data/dailyfaceoff.py` with:
    - `SLUG_TO_ABBREV` dict mapping all 32 DailyFaceoff team slugs to NHL abbreviations (see RESEARCH.md for full mapping).
    - `fetch_dailyfaceoff_starters(date_str: str) -> list[dict]`: Fetch DailyFaceoff starting-goalies page for the given date, extract `__NEXT_DATA__` JSON via regex, parse game entries. Return list of dicts with keys: home_goalie, away_goalie, home_status ("confirmed"/"likely"/"unconfirmed"), away_status, home_save_pct, away_save_pct, home_team (converted to NHL abbrev via SLUG_TO_ABBREV), away_team. Use `_status_label()` helper. Use stdlib urllib with User-Agent header and 15s timeout. All exceptions caught -- return [] on any failure. Log warnings on unexpected structure.
    - Follow the pattern from `nhl_api.py._fetch_json()` for error handling (best-effort, never crash).

    Extend `app/data/nhl_api.py` with:
    - `fetch_game_goalies(game_id: int) -> dict`: Fetch `/v1/gamecenter/{game_id}/landing`, extract `matchup.goalieComparison.{homeTeam,awayTeam}.leaders`. Return dict keyed by team abbrev, each value is list of goalie dicts with player_id, name, games_played, save_pct, gaa. Returns {} on failure. Reuse existing `_fetch_json()`.

    Create `tests/data/test_dailyfaceoff.py` with mocked HTTP responses using unittest.mock.patch on urlopen. Test all behaviors above with synthetic __NEXT_DATA__ JSON fixtures.
    Add tests for fetch_game_goalies to `tests/data/test_nhl_api.py` or a new section.
  </action>
  <verify>
    <automated>python -m pytest tests/data/test_dailyfaceoff.py tests/data/test_nhl_api.py -x -q</automated>
  </verify>
  <done>DailyFaceoff scraper returns parsed goalie confirmations from __NEXT_DATA__ JSON. NHL API fetch_game_goalies returns per-goalie stats for a game. All error paths return empty collections. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3-tier goalie resolution logic</name>
  <files>app/data/goalie_resolver.py, tests/data/test_goalie_resolver.py</files>
  <behavior>
    - Test: resolve_starter returns (goalie_dict, "confirmed") when DailyFaceoff has confirmed goalie matching team
    - Test: resolve_starter returns (goalie_dict, "likely") when DailyFaceoff has likely goalie
    - Test: resolve_starter falls back to (gp_leader_dict, "gp_leader") when DailyFaceoff has no data for team
    - Test: resolve_starter falls back to gp_leader when DailyFaceoff entry is "unconfirmed"
    - Test: resolve_starter returns (None, "none") when no goalie data exists at all
    - Test: goalie name matching works via last-name + team_code (handles "J. Swayman" vs "Jeremy Swayman")
    - Test: resolve_all_starters returns dict keyed by team_code with (goalie_dict, source) tuples for all teams in a game list
  </behavior>
  <action>
    Create `app/data/goalie_resolver.py` with:
    - `resolve_starter(team_code: str, df_starters: list[dict], goalie_stats: list[dict]) -> tuple[dict | None, str]`:
      Tier 1: Search df_starters for matching team_code where status is "confirmed" or "likely". When found, match the goalie name to goalie_stats entries by last name + team_code to get accurate season save_pct/gaa. Return (matched_goalie_dict, status).
      Tier 2: (DailyFaceoff IS tier 1 and 2 per research -- NHL API has no pregame confirmation flag.)
      Tier 3: Call `infer_likely_starter(team_code, goalie_stats)`. Return (result, "gp_leader").
      If nothing found: return (None, "none").

    - `_match_goalie_name(df_name: str, goalie_stats: list[dict], team_code: str) -> dict | None`:
      Match DailyFaceoff goalie name to goalie_stats entry. Primary match: last name + team_code. DailyFaceoff provides full names (e.g., "Jeremy Swayman"), goalie_stats has full names too. Extract last name, match against goalie_stats entries for the same team. Return matched goalie dict or None.

    - `resolve_all_starters(teams: list[str], df_starters: list[dict], goalie_stats: list[dict]) -> dict[str, tuple[dict | None, str]]`:
      Call resolve_starter for each team. Return dict keyed by team_code.

    Create `tests/data/test_goalie_resolver.py` with unit tests for all behaviors. Use synthetic goalie_stats and df_starters fixtures. No HTTP mocking needed -- this is pure logic.
  </action>
  <verify>
    <automated>python -m pytest tests/data/test_goalie_resolver.py -x -q</automated>
  </verify>
  <done>3-tier goalie resolution correctly picks confirmed > likely > gp_leader. Name matching handles full names. All edge cases (empty data, no match, unconfirmed) fall back gracefully. Tests pass.</done>
</task>

</tasks>

<verification>
python -m pytest tests/data/test_dailyfaceoff.py tests/data/test_goalie_resolver.py tests/data/test_nhl_api.py -x -q
</verification>

<success_criteria>
- DailyFaceoff scraper parses __NEXT_DATA__ JSON and returns structured goalie data
- NHL API gamecenter endpoint provides per-goalie season stats
- 3-tier resolution (confirmed > likely > gp_leader) works correctly
- All error/timeout paths degrade gracefully to empty collections
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-starting-goalies/04-01-SUMMARY.md`
</output>
