---
phase: 06-fastapi-migration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Dockerfile
  - tests/web/test_app.py
  - tests/web/test_web_preview.py
  - tests/web/test_presentation.py
  - app/web/web_preview.py
autonomous: false
requirements: [R5.1, R5.2]
must_haves:
  truths:
    - "Dockerfile CMD runs uvicorn serving FastAPI app"
    - "All existing web tests pass or are migrated to TestClient"
    - "Old web_preview.py is deleted — no stdlib HTTP server remains"
    - "Full test suite passes with no regressions"
    - "FastAPI app serves dashboard in demo mode via TestClient"
  artifacts:
    - path: "Dockerfile"
      provides: "Updated CMD for uvicorn"
      contains: "uvicorn app.web.app:app"
    - path: "tests/web/test_app.py"
      provides: "FastAPI TestClient tests for all routes"
      min_lines: 40
  key_links:
    - from: "Dockerfile"
      to: "app/web/app.py"
      via: "uvicorn CMD"
      pattern: "uvicorn app\\.web\\.app:app"
    - from: "tests/web/test_app.py"
      to: "app/web/app.py"
      via: "TestClient(app)"
      pattern: "from app\\.web\\.app import app"
---

<objective>
Complete the migration by updating Dockerfile, migrating tests to FastAPI TestClient, and deleting the old stdlib HTTP server.

Purpose: Plan 01 built the new FastAPI app alongside the old one. This plan swaps over: updates Dockerfile CMD, migrates tests, deletes web_preview.py, and verifies everything works end-to-end.

Output: Clean migration complete -- old code deleted, all tests passing, Dockerfile ready for Railway deploy.
</objective>

<execution_context>
@C:/Users/OlivierFaucher/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OlivierFaucher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fastapi-migration/06-CONTEXT.md
@.planning/phases/06-fastapi-migration/06-RESEARCH.md
@.planning/phases/06-fastapi-migration/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From app/web/app.py (created in Plan 01):
```python
from fastapi import FastAPI
app = FastAPI(title="MoneyPuck Edge Intelligence")

# Routes:
# GET / -> HTMLResponse (dashboard page)
# GET /index.html -> HTMLResponse (alias)
# GET /api/dashboard -> JSON
# GET /api/performance -> JSON
# GET /api/opportunities -> JSON
# GET /api/odds-history?game_id=X -> JSON
```

From app/web/app.py -- helper functions moved from web_preview.py:
```python
_build_demo_dashboard(params: dict[str, list[str]]) -> dict
_build_live_dashboard(params: dict[str, list[str]]) -> dict
_build_performance_data() -> dict
_build_config(params: dict[str, list[str]]) -> TrackerConfig
```

Existing tests to migrate:
- tests/web/test_web_preview.py (29 lines) -- imports _build_demo_dashboard from web_preview
- tests/web/test_presentation.py (31 lines) -- imports to_serializable and render_html_preview
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfile, migrate tests, delete old server</name>
  <files>Dockerfile, tests/web/test_app.py, tests/web/test_web_preview.py, tests/web/test_presentation.py, app/web/web_preview.py</files>
  <action>
    1. **Update Dockerfile:**
    - Change CMD from `["python", "-m", "app.web.web_preview"]` to shell form: `CMD uvicorn app.web.app:app --host 0.0.0.0 --port ${PORT:-8080}`
    - Remove `PREVIEW_HOST` and `PREVIEW_PORT` env vars from ENV block (PORT is used directly by uvicorn)
    - Keep HEALTHCHECK as-is (it hits `/` which still works)

    2. **Create `tests/web/test_app.py`** with FastAPI TestClient tests:

    ```python
    from fastapi.testclient import TestClient
    from app.web.app import app

    client = TestClient(app)
    ```

    Tests to write:
    - `test_dashboard_page_returns_html()` — `GET /?demo=1` returns 200, content-type contains "text/html", body contains "MoneyPuck Edge Intelligence"
    - `test_index_html_alias()` — `GET /index.html?demo=1` returns 200
    - `test_api_dashboard_returns_json()` — `GET /api/dashboard?demo=1` returns 200, response.json() has keys: "games", "value_bets", "books", "mode"
    - `test_api_dashboard_demo_has_games()` — verify `data["games"]` is non-empty, each game has "home", "away", "books"
    - `test_api_dashboard_demo_has_quebec_books()` — verify "Bet365", "Betway", "Bet99", "FanDuel", "Mise-o-jeu" in `data["books"]`
    - `test_api_performance_returns_json()` — `GET /api/performance` returns 200 with JSON
    - `test_api_opportunities_returns_json()` — `GET /api/opportunities?demo=1` returns 200 with JSON list
    - `test_api_odds_history_requires_game_id()` — `GET /api/odds-history` returns 400 with `{"error": "game_id parameter required"}`
    - `test_api_odds_history_demo_mode()` — `GET /api/odds-history?game_id=MTL-TOR-2026-01-01&demo=1` returns 200 with "snapshots" key
    - `test_security_headers()` — `GET /?demo=1` response has all 4 security headers (X-Content-Type-Options, X-Frame-Options, Content-Security-Policy, Referrer-Policy)
    - `test_404_returns_json()` — `GET /nonexistent` returns 404

    3. **Update `tests/web/test_web_preview.py`:**
    - Change import from `from app.web.web_preview import _build_demo_dashboard` to `from app.web.app import _build_demo_dashboard`
    - Both existing tests (`test_demo_dashboard_shape`, `test_demo_dashboard_has_quebec_books`) stay exactly the same, only the import changes

    4. **Update `tests/web/test_presentation.py`:**
    - Keep as-is. It imports `to_serializable` and `render_html_preview` from `app.web.presentation` which still exists unchanged from Plan 01.
    - Only update if Plan 01 changed presentation.py imports. If `render_dashboard` was kept in presentation.py, no changes needed.

    5. **Delete `app/web/web_preview.py`** — the old stdlib HTTP server. All its business logic functions now live in `app/web/app.py`.

    6. **Check for other imports of web_preview** across the codebase:
    ```bash
    grep -r "web_preview" --include="*.py" . | grep -v __pycache__ | grep -v ".pyc"
    ```
    Update any remaining references. Likely places:
    - `tracker.py` — check if it imports anything from web_preview (it may import `start_server` or similar). If so, update to import from app.web.app or remove if no longer needed (Dockerfile handles server start).
    - Any `__init__.py` files that re-export web_preview symbols.

    7. Run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Dockerfile uses uvicorn CMD. All tests pass including new TestClient tests. web_preview.py deleted. No remaining imports of web_preview anywhere. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of FastAPI dashboard</name>
  <action>Human verifies dashboard renders identically after migration.</action>
  <what-built>Complete FastAPI migration: new app.py with all routes, Jinja2 template, updated Dockerfile, migrated tests, old server deleted.</what-built>
  <how-to-verify>
    1. Start the FastAPI server locally:
       ```
       pip install -r requirements.txt
       uvicorn app.web.app:app --host 0.0.0.0 --port 8080
       ```
    2. Visit http://localhost:8080?demo=1 — dashboard should look identical to before
    3. Visit http://localhost:8080/api/dashboard?demo=1 — should return JSON with games/value_bets
    4. Visit http://localhost:8080/api/performance — should return JSON
    5. Visit http://localhost:8080/api/odds-history?game_id=MTL-TOR-2026-01-01&demo=1 — should return sparkline data
    6. Check that interactive features (tabs, tooltips, sparklines) still work in the dashboard
    7. Verify the visual appearance is identical — same colors, fonts, layout
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any visual/functional differences</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` — full suite passes
- `python -m pytest tests/web/ -x -q` — all web tests pass
- `grep -r "web_preview" --include="*.py" . | grep -v __pycache__` — no remaining references
- `python -c "from app.web.app import app; print('OK')"` — import succeeds
- Dockerfile contains `uvicorn app.web.app:app`
</verification>

<success_criteria>
- Old web_preview.py is deleted
- Dockerfile CMD uses uvicorn
- All new TestClient tests pass (11+ tests)
- All existing tests pass (no regressions)
- Dashboard renders identically in browser (human-verified)
- No remaining imports of web_preview in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/06-fastapi-migration/06-02-SUMMARY.md`
</output>
